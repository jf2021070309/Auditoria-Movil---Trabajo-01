#include "../commons/uniforms.sh"

varying	highp	vec2 	V_TexCoord0;
varying	lowp	vec4	V_VertexColor;

void main()
{
	lowp vec4 color = texture2D( BASEMAP, V_TexCoord0 );
	lowp vec4 diff = V_VertexColor;

	gl_FragColor = vec4( color.xyz * diff.xyz, color.w ) * diff.w;
}
